graceful shutdown

想一下，对于一个机器，在做代码变更操作需要重启时，肯定存在一个时间段该机器无法处理任何请求，其实还有一个时间段，就是请求已经请求到了机器、机器正在处理，但是此时进程被杀死了，那么这个请求对上游该怎么办呢？实际上，进程在接收到终止之前，会先主动的停止接收请求，等待一段时间，保证之前已经进入的请求已经完成，然后再真的停止进程。但是其实还有一个问题哈，就是服务的某些请求本来的请求时间就是比较长，怎么办呢？
