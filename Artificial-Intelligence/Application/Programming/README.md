+ AI编程工具可以分成两类：
    + AI-Code&Tab：比如VSCode的Copilot的Tab，比如Cursor的Tab
    + Agent：
        + 编辑器：VSCode的Copilot的Agent，Cursor的Agent
        + 终端：sst opencode


```bash
sudo chown -R $(whoami) ~/Library/Application\ Support/Cursor
sudo chown -R $(whoami) ~/.cursor
# chmod -R u+rw ~/Library/Application\ Support/Cursor
# chmod -R u+rw ~/.cursor
```